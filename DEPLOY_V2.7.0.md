# Deploy v2.7.0

Apply this patch only after v2.6.0. Run `npm install`, `npm run check` and `npm run deploy:dry-run`. Commit the Phase 2 checkpoint on the feature branch. Do not merge to `main` until Phase 3 is complete.
