{
  "name": "trail-runner-coach",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Local-first adaptive trail training coach with expanded Thai food logging, calorie deficit filters, strain/recovery and wearable connection architecture",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test",
    "check": "node scripts/verify.mjs && node --test",
    "deploy:dry-run": "npx wrangler deploy --dry-run"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^29.1.1",
    "wrangler": "4.103.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
