{
  "name": "trail-runner-coach",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first bilingual adaptive trail coach with Apple Health, Google Health/Fitbit, Strava sync, personalized scores, analytics and encrypted cloud backup",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test",
    "check": "node scripts/verify.mjs && node --test",
    "deploy:dry-run": "npx wrangler deploy --dry-run",
    "setup:strava": "node scripts/setup-strava.mjs",
    "setup:backup": "node scripts/setup-cloud-backup.mjs",
    "setup:google-health": "node scripts/setup-google-health.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^29.1.1",
    "wrangler": "4.103.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
